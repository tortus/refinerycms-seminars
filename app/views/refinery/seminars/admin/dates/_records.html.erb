<%= render 'refinery/admin/search_header', :url => refinery.seminars_admin_seminar_dates_path %>
<div class='pagination_container'>
  <% if @seminar_dates.any? %>
    <%= render 'seminar_dates' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
